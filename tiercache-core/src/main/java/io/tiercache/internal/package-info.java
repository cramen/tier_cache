/**
 * Engine internals: the default cache implementations, the L2 circuit
 * breaker, TTL jitter, and startup configuration validation.
 *
 * <p><b>Internal — not part of the supported API.</b> Every type in this
 * package may change in any release without notice.
 *
 * @since 0.1.0
 */
package io.tiercache.internal;

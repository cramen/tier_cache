upstream demo {
    server app1:8080;
    server app2:8080;
    server app3:8080;
}

server {
    listen 8080;

    location / {
        proxy_pass http://demo;
        proxy_connect_timeout 5s;
        proxy_read_timeout 30s;
    }
}
